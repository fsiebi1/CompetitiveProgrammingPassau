#include <bits/stdc++.h>
#define MAX_N 2'000'000
#define MAX_H 500'005

using namespace std;

int h, n;
int arr[MAX_H];

int main() {
	int t;
	cin >> t;

    for(; t > 0; t--) {
        cin >> h >> n;

        for(int i = 0; i < h; i++)
            scanf("%d", arr + i);

        
    }
}